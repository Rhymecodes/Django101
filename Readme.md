- Create a virtual environment
    * python -m venv env 
    * source ./env/Scripts/activate
- Install Django within the virtual environment (pip intall Django)
- Create a project 'django-admin startproject projectname
